# Python and Java projects
1. The first program, Gamelist, is a program in which users can add, view, search, update, and delete the games they have played.
   
2. The second, graphweightededges, is a program that generated graph with edge weights.

3. The third, MaxHeapsort, is a program that sorts three files according to MaxHeap.

4. The fourth, is program that sorts a users luggage according to their class and generates a unique ID for each luggae

5. The first is Java program that applies OOP to make flashcards.

6. The second while it does at apply OOP, it does use data structures and algorithms, by making a carpet calculator for the user according to how many square feet and price per rool.

7. The FIFO program is a chat program between two termainals which uses named pipes (FIFO) for inter-process communication (IPC). The named pipes provided continous communication between process and exits until it is deleted.

8. The log_analyzer is multithreaded log analyzer using the classic producer and consumer pattern. There is one producer thread that reads shared log lines into a shared bounded queue. Followed with N number of consumer, the N is dependent on the user, that will consume the log lines and update the shared statistics.
